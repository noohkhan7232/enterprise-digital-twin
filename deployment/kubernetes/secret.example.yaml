# TEMPLATE ONLY — do not commit real secrets. Copy to secret.yaml, fill values,
# and apply out-of-band (or supply via an external secrets manager / sealed
# secrets). Values below are placeholders and must be replaced.
apiVersion: v1
kind: Secret
metadata:
  name: edt-secrets
  namespace: enterprise-digital-twin
  labels:
    app.kubernetes.io/name: enterprise-digital-twin
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME"
  MODEL_REGISTRY_TOKEN: "REPLACE_ME"
  MONITORING_API_KEY: "REPLACE_ME"