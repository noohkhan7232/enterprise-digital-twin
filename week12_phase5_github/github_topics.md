# Suggested GitHub Topics

Topics improve discoverability. GitHub allows up to 20 topics per repository; each must be lowercase,
may include hyphens, and should be specific and accurate. The list below is ordered by relevance.
Choose up to 20; the first 20 here are within the limit.

---

## Recommended (≤ 20)

```
digital-twin
decision-intelligence
mlops
observability
site-reliability-engineering
machine-learning-operations
retrieval-augmented-generation
agentic-ai
industrial-ai
industry-40
kubernetes
docker
python
software-architecture
production-ml
model-monitoring
ci-cd
reliability-engineering
enterprise-architecture
reference-architecture
```

## Rationale

| Topic | Why it fits |
|-------|-------------|
| `digital-twin` | Core capability layer |
| `decision-intelligence` | Platform framing and goal |
| `mlops` / `machine-learning-operations` | Implemented MLOps subsystem |
| `observability` / `site-reliability-engineering` / `reliability-engineering` | Observability subsystem and SLO/incident discipline |
| `retrieval-augmented-generation` | Knowledge layer |
| `agentic-ai` | Agentic layer |
| `industrial-ai` / `industry-40` | Target domain |
| `kubernetes` / `docker` | Deployment substrate |
| `python` | Implementation language |
| `software-architecture` / `enterprise-architecture` / `reference-architecture` | The project's primary contribution |
| `production-ml` / `model-monitoring` | Production-engineering focus |
| `ci-cd` | CI/CD subsystem |

## Notes

- Keep topics accurate; avoid topics implying capabilities the repository does not implement in code
  (for example, do not add framework-specific topics the project does not use).
- If GitHub's topic limit or suggestions differ at publish time, prioritise the first ten, which capture
  the platform's identity most precisely.
